server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      # 与服务注册于发现组件进行结合，通过 serviceId 转发到具体的服务实例
      discovery:
        locator:
          enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/
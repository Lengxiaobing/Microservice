server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-center
      profile: dev
      # 设置为配置中心的路径
      label:

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/

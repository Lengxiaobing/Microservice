server:
  port: 8030

spring:
  application:
    name: message-service-bus
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-center-bus
      profile: dev
      # 设置为配置中心的路径
      label:

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/

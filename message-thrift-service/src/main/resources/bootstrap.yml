server:
  port: 8030

spring:
  application:
    name: message-thrift-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-center
      profile: dev
      label:

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/

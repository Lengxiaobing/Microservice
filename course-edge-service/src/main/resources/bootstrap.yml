server:
  port: 8035

spring:
  application:
    name: course-edge-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-center
      profile: dev
      label:

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/

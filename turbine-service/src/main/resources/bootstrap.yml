server:
  port: 8032

spring:
  application:
    name: turbine-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-center
      profile: dev
      label:

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/
